## 🌟 ฟีเจอร์หลัก (Key Features)

* 🔍 **Smart Search System:** ค้นหารูปภาพผ่านคำค้นหา (Query String) ด้วย Fetch API
* 📦 **Dynamic Load More:** ระบบโหลดรูปภาพเพิ่มครั้งละ 6 รูปมาต่อท้ายรายการเดิม (Pagination)
* ❤️ **Interactive Heart System:** ปุ่มกดถูกใจด้านล่างภาพ พร้อม Animation "Pop" เมื่อกดใช้งาน
* 🖼️ **Perfect Grid Layout:** จัดเรียงรูปแบบ 3x2 (หรือตามจำนวนรูป) โดยใช้ CSS Grid และรักษาสัดส่วนภาพให้เท่ากันทุกใบ
* 📱 **Fully Responsive:** ปรับเปลี่ยนเลย์เอาต์ตามขนาดหน้าจอ (Desktop, Tablet, Mobile)
* ✨ **Premium Lightbox:** คลิกเพื่อดูภาพขนาดเต็ม (High-Res) พร้อมพื้นหลังเบลอ (Backdrop Blur)

---

## 🛠 ทักษะการพัฒนาที่แสดงในโปรเจกต์นี้ (Technical Expertise)

### 1. การจัดการข้อมูล (Data Handling)

* **Fetch API:** การใช้ `async/await` เพื่อดึงข้อมูลจาก Unsplash API
* **JSON Processing:** การจัดการโครงสร้างข้อมูลแบบซ้อนเพื่อดึง URL ภาพ, ชื่อช่างภาพ และข้อมูลโปรไฟล์
* **Query Parameters:** การจัดการพารามิเตอร์ใน URL เช่น `query`, `page`, และ `per_page`

### 2. ตรรกะของ Vue 3 (Vue Logic)

* **Composition API:** แยก Logic ออกเป็นไฟล์ `logic.js` เพื่อความเป็นระเบียบ (Reusability)
* **Reactive State:** การใช้ `ref` จัดการสถานะ Loading, Error และผลลัพธ์รูปภาพ
* **List Rendering:** การใช้ `v-for` เพื่อสร้างการ์ดรูปภาพตามจำนวนข้อมูลจริง

### 3. การตกแต่งขั้นสูง (Advanced UI/UX)

* **CSS Aspect Ratio:** บังคับให้รูปภาพทุกรูปมีขนาดเท่ากันเป๊ะ (4:3) เพื่อความสวยงามของ Grid
* **Animations:** ใช้ `@keyframes` สำหรับปุ่ม Like และ Vue `<Transition>` สำหรับ Modal
* **Clean Design:** เน้นการใช้สีขาวและฟ้า เพื่อความสบายตาของผู้ใช้งาน

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
├── src/
│   ├── style.css
│   ├── logic.js
│   └── App.vue        
└── README.md      

```

---

## 🚀 เริ่มต้นใช้งาน (Getting Started)

1. **Clone the project**
```bash
git clone https://github.com/your-username/Lab_exam_1_NECS1205/.git

```


2. **Insert your Unsplash API Key**
เปิดไฟล์ `logic.js` และนำ **Access Key** ของคุณไปใส่ที่ตัวแปร `ACCESS_KEY`
3. **Install & Run**
```bash
npm install
npm run dev

```



---

## 📷 ตัวอย่างการแสดงผล (Preview)

> **"ค้นหาคำว่า 'Fox white' ระบบจะดึงรูปภาพ 6 รูปแรกมาจัดวางในรูปแบบ Grid 3 คอลัมน์ พร้อมแสดงชื่อช่างภาพและปุ่มกดใจด้านล่าง เมื่อคลิกที่รูปจะขยายเป็นภาพต้นฉบับความละเอียดสูงทันที"**

---
